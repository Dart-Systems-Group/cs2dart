/// The output kind of a compiled C# project.
enum OutputKind {
  /// A console or command-line executable.
  exe,

  /// A class library (DLL).
  library,

  /// A Windows GUI executable.
  winExe,
}
