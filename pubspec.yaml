name: cs2dart
description: A C# to Dart transpiler.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  yaml: ^3.1.2
  xml: ^6.5.0

dev_dependencies:
  test: ^1.24.0
  build: ^4.0.5
  build_runner: ^2.13.1
